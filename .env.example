# IMAP Server Settings
# These are the credentials for the "catch-all" email account.
IMAP_HOST=your_imap_server.com
IMAP_USER=your_email@yourdomain.com
IMAP_PASS="your_email_password"
IMAP_PORT=993

# Domain Configuration
# This is the domain that will be used for generating temporary email addresses.
# It should be the same domain that you have configured the "Default Address" for in cPanel.
DOMAIN=yourdomain.com

# Cleanup Job Security
# A secret key to prevent unauthorized access to the cleanup endpoint.
# Use a long, random string.
CLEANUP_SECRET="a_very_secret_key"
